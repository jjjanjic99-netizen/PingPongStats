<Window x:Class="PingPongStats.App.Views.DataPathSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PingPongStats - Datenordner einrichten"
        Width="560" Height="300"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource Brush.Background}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Willkommen bei PingPongStats" Style="{StaticResource HeadingText}" />

        <TextBlock Grid.Row="1" Margin="0,8,0,16" TextWrapping="Wrap"
                   Foreground="{DynamicResource Brush.TextSecondary}"
                   Text="Bitte wählen Sie einen Ordner, in dem die Spieler- und Spieldaten gespeichert werden sollen. Das kann ein lokaler Ordner (z. B. C:\PingPongStats\Data) oder ein Netzwerkpfad sein (z. B. \\fileserver\PingPongStats\), damit mehrere Kolleg:innen dieselben Daten nutzen können." />

        <TextBlock Grid.Row="2" Text="Datenordner" Style="{StaticResource SectionLabelText}" Margin="0,0,0,4" />

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding SelectedPath, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="1" Content="Durchsuchen…" Style="{StaticResource SecondaryButton}"
                    Margin="8,0,0,0" Command="{Binding BrowseCommand}" />
        </Grid>

        <TextBlock Grid.Row="4" Margin="0,12,0,0" TextWrapping="Wrap"
                   Foreground="{DynamicResource Brush.Danger}"
                   Text="{Binding ErrorMessage}"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Abbrechen" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"
                    Click="OnCancelClick" />
            <Button Content="Bestätigen" Style="{StaticResource PrimaryButton}" Command="{Binding ConfirmCommand}" />
        </StackPanel>
    </Grid>
</Window>
