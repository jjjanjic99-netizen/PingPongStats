<UserControl x:Class="PingPongStats.App.Controls.AvatarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Mockup .av: circular, Display/Bold initials in dark-on-pastel, optional
         ring border at L (subtle white) and XL (solid Ball orange) - drawn as
         each Ellipse's own Stroke rather than a separate overlay element. -->
    <Grid x:Name="RootGrid">
        <Ellipse x:Name="FallbackEllipse" />
        <TextBlock x:Name="InitialsText" HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontFamily="{DynamicResource Font.Display}" FontWeight="Bold"
                   Foreground="{DynamicResource Brush.OnBall}" />
        <Ellipse x:Name="ImageEllipse" Visibility="Collapsed">
            <Ellipse.Fill>
                <ImageBrush x:Name="AvatarImageBrush" Stretch="UniformToFill" />
            </Ellipse.Fill>
        </Ellipse>
    </Grid>
</UserControl>
